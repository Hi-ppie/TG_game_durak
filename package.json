{
  "name": "tg-game-durak",
  "private": true,
  "packageManager": "pnpm@9",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "concurrently -n server,webapp -c green,blue \"pnpm --filter server dev\" \"pnpm --filter webapp dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "vite": "^7.3.0"
  }
}
