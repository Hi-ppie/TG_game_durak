{
  "name": "webapp",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo \"(добавим Vite позже)\"",
    "build": "echo \"(добавим сборку позже)\"",
    "preview": "echo \"(добавим предпросмотр позже)\""
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}